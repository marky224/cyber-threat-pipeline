[project]
name = "cyber-threat-pipeline-transform"
version = "0.1.0"
description = "Isolated dbt env for the cyber-threat-pipeline transform stage."
requires-python = ">=3.12,<3.13"
dependencies = [
    "dbt-core>=1.9,<2",
    "dbt-postgres>=1.9,<2",
]

[tool.uv]
managed = true
